[project]
name = "cosyvoice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torchaudio>=2.10.0",
    "torchcodec>=0.10.0",
]
